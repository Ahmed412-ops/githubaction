name: First workflow

on:
  push:
    branches:
     - main
     
jobs:
  example-jobs:
    runs-on: ubuntu-latest

    steps:
     - name: print welcome message
       run: echo "welcome to workflow agaain"
       
